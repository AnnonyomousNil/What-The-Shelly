# What The Shelly 🐚

What the shelly is a minimal Unix-like shell written in Python.

## Features
- Builtin commands: echo, exit, pwd, cd, type
- PATH-based external command execution
- Single-quote parsing
- Home directory (~) support

## Usage
```bash
python whattheshelly.py
